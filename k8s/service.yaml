apiVersion: v1
kind: Service
metadata:
  name: hello-flask-svc
spec:
  type: NodePort
  selector:
    app: hello-flask
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 31555

